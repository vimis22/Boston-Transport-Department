FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY base_streamer.py .
COPY weather_streamer.py .

# Create data directory
RUN mkdir -p /data

# Run application
CMD ["python", "weather_streamer.py"]
