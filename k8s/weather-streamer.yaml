apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-streamer
  namespace: boston-transport
  labels:
    app: weather-streamer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-streamer
  template:
    metadata:
      labels:
        app: weather-streamer
    spec:
      containers:
      - name: weather-streamer
        image: boston-transport/weather-streamer:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: TIME_MANAGER_URL
          value: "http://time-manager:5000"
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: DATA_FILE
          value: "/data/weather_observations.csv"
        - name: POLL_INTERVAL
          value: "1.0"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: mock-data
          mountPath: /data
          readOnly: true
      volumes:
      - name: mock-data
        configMap:
          name: mock-data
